{
  "name": "macokasa-kabaza-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/write-config.mjs",
    "check": "node --check src/app.js && node --check src/data.js && node --check scripts/dev-server.mjs && node --check scripts/write-config.mjs && node --check scripts/security-check.mjs",
    "security": "node scripts/security-check.mjs",
    "verify": "npm run check && npm run build && npm run security && npm test",
    "test": "node tests/finance.test.mjs",
    "sqlcheck": "python3 scripts/validate-sql.py"
  }
}
